#include<stdio.h>

int main()
{
    int a[10],i;

    printf("Enter 10 numbers:\n");

    for(i=0;i<10;i++)
        scanf("%d",&a[i]);

    printf("Even Numbers:\n");
    for(i=0;i<10;i++)
    {
        if(a[i]%2==0)
            printf("%d ",a[i]);
    }

    printf("\nOdd Numbers:\n");
    for(i=0;i<10;i++)
    {
        if(a[i]%2!=0)
            printf("%d ",a[i]);
    }

    return 0;
}
